---
description: "Release-readiness gate for merge, ship, deploy, publish, tag, or handoff. Modes: --dry-run, --fast, --release, --rollback."
argument-hint: "[--dry-run | --fast | --release | --rollback] [plan-or-scope]"
---

Load the `ck:ship` skill and run it with `$ARGUMENTS`.
